//program to print meow 3 times

#include <stdio.h>

int main(void)
{
    //sets the intial value of counter
    int counter=0;

    //program to loop n times
    while(counter<3)
    {
        printf("meow \n");
        counter ++;
    }
}